{
  "name": "cct-uni-web",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vite",
    "audit:deep": "node scripts/deep-audit.mjs",
    "check:frontend": "node scripts/check-frontend-readiness.mjs",
    "check:architecture": "node scripts/check-architecture.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "check:browser": "node scripts/check-browser.mjs",
    "check:all": "npm run audit:deep && npm run build && npm run check:browser",
    "build": "npm run check:frontend && npm run check:architecture && tsc --noEmit && vite build && npm run check:dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vite-plugin-static-copy": "^1.0.6"
  }
}
